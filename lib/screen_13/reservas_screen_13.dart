import 'package:flutter/material.dart';

class ReservasScreen13 extends StatelessWidget {
  const ReservasScreen13({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Reservas'),
      ),
    );
  }
}
